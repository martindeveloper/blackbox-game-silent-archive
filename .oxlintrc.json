{
  "rules": {
    "no-restricted-imports": [
      "warn",
      {
        "patterns": [
          {
            "group": ["@engine/**", "!@engine/v1/**"],
            "message": "Use the stable @engine/v1/* API. Raw @engine/* modules are internal and may change without a version bump."
          }
        ]
      }
    ]
  }
}
